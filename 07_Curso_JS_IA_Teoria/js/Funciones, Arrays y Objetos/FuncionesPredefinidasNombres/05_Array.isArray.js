// 5. Array.isArray

// Verifica si un valor es un array.
let numeros = [1, 2, 3];
let texto = "Hola";

// Verificar si es un array
console.log(Array.isArray(numeros)); // true
console.log(Array.isArray(texto)); // false

// Ideal para asegurarte de que una variable es un array antes de trabajar con sus métodos.