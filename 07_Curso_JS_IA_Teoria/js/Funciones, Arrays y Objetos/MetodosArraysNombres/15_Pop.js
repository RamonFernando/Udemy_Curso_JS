let numeros = [1, 2, 3, 4];

// Eliminar el último elemento
let ultimo = numeros.pop();
console.log(ultimo); // 4
console.log(numeros); // [1, 2, 3]

// pop modifica el array original eliminando el último elemento y devuelve ese elemento.