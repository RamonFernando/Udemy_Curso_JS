let numeros = [1, 2, 3];
let texto = "Hola";

// Verificar si son arrays
console.log(Array.isArray(numeros)); // true
console.log(Array.isArray(texto)); // false

// isArray devuelve true si el valor es un array y false si no lo es.