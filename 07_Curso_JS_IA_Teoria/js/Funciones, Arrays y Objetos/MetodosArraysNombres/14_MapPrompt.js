// Crear un array de números
/*let numeros = [1, 2, 3, 4, 5];

// Pedir al usuario que ingrese la potencia
let potencia = parseInt(prompt("Ingresa la potencia a la que deseas elevar los números:"));

// Elevar cada número a la potencia ingresada
let resultadosPotencia = numeros.map((num) => num ** potencia);
console.log(`Números elevados a la potencia ${potencia}:`, resultadosPotencia);*/


// Crear un array de números
let numeros = [1, 2, 3, 4, 5];

// Simular el ingreso del usuario (reemplázalo con cualquier valor)
let potencia = 3; // Cambia esto por el valor deseado

// Elevar cada número a la potencia ingresada
let resultados = numeros.map((num) => num ** potencia);
console.log(`Números elevados a la potencia ${potencia}:`, resultados);

