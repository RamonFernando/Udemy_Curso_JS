package Tema1Variables;

/**
 * @author Ramon
 *
 */
public class Caracteres_y_Metodos {

	/**
	 * Pildoras Informaticas
	 */
	public static void main(String[] args) {
		// Teoria
	
	/** Bibliotecla
	 *  API Java -- Es la biblioteca de java
	 *  Pagina web: docs.oracle.com/javase/7/docs/api/      Para java 7
	 *  			docs.oracle.com/javase/8/docs/api/      Para java 8
	 * 
	 * 
	 * 
	 *   Para caracteres:
	 *  String nombre = Ramon;
	 *   int ultimaLetra;
	 *  
	 *   charAt(); Para indicar la posicion de un nombre o parrafo que esta la letra Ej
	 *   nombre.charAt(7);
	 *   
	 *   Length Para comprobar toda la cantidad de letras que tiene un nombre por ejemplo.
	 *   
	 *   nombre.length comprueba toodos los caracteres que tiene. Te devolveria por consola
	 *   Mi nombre es Ramon y tiene 5 letras.
	 *   Para saber la ultima letra seria: 
	 *   nombre.charAt(ultimaLetra-1); el -1 es por que la cuenta siempre toma como primer valor el cero. si no pusieramos el menos -1 no nos daria ningun valor.
	 *   
	 *   
	 *   Metodo Math.
	 *   
	 *   Math.pow(base,exponente); es para el elevar a potencia primero el exponente y luego la potencia.
	 *   Math.sqrt(); es para la raiz cuadrada
	 * 	 Math.round(); es para redondear o troncar los decimales.
	 *   long raiz;
	 *   int raiz = (int)Math.round(num1); aqui se convierte un long a un int. Se le llama refundicion.
	 *   
	 * 
	 * 
	 * 
	 */
	
	
	
	
	
	}

}
