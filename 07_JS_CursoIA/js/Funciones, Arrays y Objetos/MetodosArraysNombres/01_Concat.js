let numeros1 = [1, 2, 3];
let numeros2 = [4, 5, 6];

// Combinar los arrays
let combinados = numeros1.concat(numeros2);
console.log(combinados); // [1, 2, 3, 4, 5, 6]

// concat no modifica los arrays originales, sino que devuelve un nuevo array combinado.